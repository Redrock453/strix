# 🔧 Туннель для доступа к Strix

## ✅ Текущий статус
- **IP адрес**: 10.0.15.231
- **Тестовый сервер**: http://localhost:8000 (запущен)
- **SSH порт**: 2222
- **Рабочий порт**: 8000

## 🚀 Быстрый старт

### 1. Проверка работы (уже запущено!)
```bash
curl http://localhost:8000/api/status
```

### 2. Варианты доступа из вашей локальной сети

#### Вариант A: GitHub Codespaces Port Forwarding (Рекомендуется)
```bash
gh codespace ports forward 8000:8000
```
Затем открыть в браузере: `http://localhost:8000`

#### Вариант B: SSH Local Port Forwarding
С вашей локальной машины:
```bash
ssh -L 8000:localhost:8000 -p 2222 user@10.0.15.231
```

#### Вариант C: Ngrok (публичный доступ)
```bash
docker run -it --rm --net=host ngrok/ngrok:latest http 8000
```

#### Вариант D: Cloudflare Tunnel (публичный доступ)
```bash
docker run -it --rm cloudflare/cloudflared:latest tunnel --url http://localhost:8000
```

## 📋 Управление тестовым сервером

### Запустить тестовый сервер
```bash
cd /workspaces/strix
python test_tunnel_server.py 8000 &
```

### Остановить тестовый сервер
```bash
pkill -f test_tunnel_server
```

### Проверить, запущен ли сервер
```bash
netstat -tlnp 2>/dev/null | grep :8000
# или
ss -tlnp | grep :8000
# или
lsof -i :8000
```

## 🦉 Запуск Strix через туннель

### Вариант 1: Tool Server (для тестирования)
```bash
cd /workspaces/strix
export SANDBOX_MODE=1
python -m strix.runtime.tool_server --host 0.0.0.0 --port 8000 --token your-secret-token
```

### Вариант 2: Полный Strix Agent
```bash
cd /workspaces/strix
python -m strix.interface.main --target https://example.com --non-interactive
```

## 🔍 Полезные команды

### Показать сетевую информацию
```bash
/workspaces/strix/tunnel_setup.sh info
```

### Показать варианты публичного доступа
```bash
/workspaces/strix/tunnel_setup.sh expose
```

### Запустить тестовый HTTP сервер
```bash
/workspaces/strix/tunnel_setup.sh test
```

## 🌐 Доступ к тестовому серверу

После запуска туннеля, доступны следующие эндпоинты:

- **GET /** - HTML страница с информацией о подключении
- **GET /api/status** - JSON статус сервера
- **GET /api/health** - Health check endpoint

Пример:
```bash
curl http://localhost:8000/api/status
```

## 🔐 Безопасность

⚠️ **Важно**: 
- Тестовый сервер не имеет авторизации
- Используйте только для разработки/тестирования
- Для production используйте HTTPS и аутентификацию
- При использовании ngrok/cloudflare получите публичный URL - будьте осторожны!

## 📊 Автоматическое одобрение команд (настроено)

В VS Code Copilot автоматически одобрены следующие команды:
- ✅ Проверка статуса сервера (curl)
- ✅ Запуск/остановка тестового сервера
- ✅ Проверка сетевых портов
- ✅ Скрипты tunnel_setup.sh

## 🎯 Что дальше?

1. **Для локального тестирования**: Используйте GitHub Codespaces port forwarding
2. **Для удалённого доступа**: Используйте SSH туннель
3. **Для публичного доступа**: Используйте ngrok или cloudflare tunnel
4. **Для production**: Настройте proper reverse proxy (nginx/caddy) с HTTPS

---

💡 **Подсказка**: Все команды уже добавлены в auto-approve, так что Copilot будет выполнять их автоматически!
